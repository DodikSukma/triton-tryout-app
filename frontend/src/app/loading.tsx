import TritonLoader from '@/components/common/TritonLoader'

// Global App Router fallback shown during route segment transitions.
export default function Loading() {
  return <TritonLoader fullScreen />
}
